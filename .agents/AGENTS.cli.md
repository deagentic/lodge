# CLI Context (AGENTS.cli.md)

You are operating within the CLI tooling context.
- Use `version-manager` for release bumps.
- Follow POSIX conventions for command-line arguments and standard output/error.
